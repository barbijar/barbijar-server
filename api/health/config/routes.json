{
  "routes": [
    {
      "method": "GET",
      "path": "/health",
      "handler": "health.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/health/count",
      "handler": "health.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/health/:id",
      "handler": "health.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/health",
      "handler": "health.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/health/:id",
      "handler": "health.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/health/:id",
      "handler": "health.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
